---
description: List every hostname currently routed by the harker.systems Cloudflare tunnel
allowed-tools: Bash
---

Show the live tunnel routing table. Run:

```bash
cd /Users/stu/Projects/tmux-setup/cloudflare && python3 list_services.py
```

Reproduce the output verbatim so I can grep / scan it.
